# Shared environment for backend + frontend
# Copy this file to `.env` in the repo root and fill real values.

# --------------------------
# Backend (private secrets)
# --------------------------
PORT=4000
MONGODB_URI=mongodb://127.0.0.1:27017/bubble

JWT_ACCESS_SECRET=change_me_access
JWT_REFRESH_SECRET=change_me_refresh
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=30d

ADMIN_EMAILS=admin1@example.com,admin2@example.com

EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_gmail_app_password
EMAIL_FROM="Bubble <your_email@gmail.com>"

SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

BACKEND_PUBLIC_URL=http://localhost:4000
BACKEND_PUBLIC_URLS=
APP_DEEPLINK_URL=Bubble://
EMAIL_SHOW_LOGO=true
EMAIL_LOGO_URL=

# --------------------------
# Frontend (public only)
# --------------------------
# Expo only exposes keys prefixed with EXPO_PUBLIC_ to the app bundle.
EXPO_PUBLIC_API_URL=http://localhost:4000/api
