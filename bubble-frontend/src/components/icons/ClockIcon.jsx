import React from "react";
import { MaterialIcons } from "@expo/vector-icons";

export default function ClockIcon({ color = "#8E8E8E", size = 20 }) {
  return <MaterialIcons name="access-time" size={size} color={color} />;
}
